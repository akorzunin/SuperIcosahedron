# Validation

For changes affecting gameplay presentation, input, cameras, UI, or shaders:

- Run `task test` (GUT unit/integration tests).
- Run `task visual-playtest` before and after the change to ensure visual validation.
